<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Step Log" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700;800&family=Noto+Sans+JP:wght@400;500;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght@400&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-[#f0f7fa] text-[#111618]">
    <div class="pointer-events-none fixed -top-16 -left-10 h-56 w-56 rounded-full bg-[#B3E5FC]/70 blur-3xl"></div>
    <div class="pointer-events-none fixed -right-8 top-40 h-64 w-64 rounded-full bg-[#80DEEA]/60 blur-3xl"></div>

    <main class="relative z-10 mx-auto w-full max-w-3xl px-4 pb-10 pt-4 sm:pt-6">
      <%= render "shared/header" %>

      <% if flash.any? %>
        <div class="mb-4 space-y-2">
          <% flash.each do |type, message| %>
            <% next if message.blank? %>
            <% style = type.to_sym == :notice ? "bg-[#E6FFFA] border-[#80DEEA] text-[#075985]" : "bg-[#FFF1F2] border-[#FDA4AF] text-[#9F1239]" %>
            <div class="rounded-xl border px-4 py-3 text-sm font-medium shadow-sm <%= style %>">
              <%= message %>
            </div>
          <% end %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
