<header class="mb-10 rounded-[2rem] border border-white/70 bg-white/80 px-5 py-4 shadow-sm backdrop-blur dark:border-slate-700 dark:bg-slate-800/80">
  <div class="flex flex-wrap items-center justify-between gap-3">
    <div class="flex flex-wrap items-center gap-2 sm:gap-3">
      <% unless current_page?(root_path) %>
        <%= link_to root_path, class: "inline-flex h-9 items-center gap-1 rounded-xl border border-slate-200 px-3 text-sm font-bold text-slate-700 hover:bg-slate-50 dark:border-slate-600 dark:text-slate-200 dark:hover:bg-slate-700" do %>
          <span aria-hidden="true">←</span>
          <span>戻る</span>
        <% end %>
      <% end %>

      <%= link_to root_path, class: "inline-flex items-center gap-2 rounded-xl px-2 py-1 hover:bg-slate-50 dark:hover:bg-slate-700" do %>
        <div class="grid size-10 place-items-center rounded-xl bg-[#38bdf8] text-white">
          <span class="icon text-[18px]">task_alt</span>
        </div>
        <span class="font-logo text-2xl font-bold tracking-tight text-slate-900 dark:text-white">StepLog</span>
      <% end %>
      <span class="inline-flex items-center rounded-full border border-slate-100 bg-slate-50 px-4 py-2 text-sm font-semibold text-slate-700 dark:border-slate-600 dark:bg-slate-700 dark:text-slate-100">
        🔥 <%= header_achieved_days %>日
      </span>
      <%= link_to "履歴", records_path, class: "inline-flex h-10 items-center rounded-full border border-slate-200 px-6 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-600 dark:text-slate-100 dark:hover:bg-slate-700" %>
    </div>

    <% if current_habit %>
      <%= link_to "習慣の設定", edit_habit_path, class: "inline-flex h-10 items-center rounded-full border border-slate-200 px-5 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-600 dark:text-slate-100 dark:hover:bg-slate-700" %>
    <% end %>
  </div>
</header>
