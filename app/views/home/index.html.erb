<div class="max-w-md mx-auto mt-10 p-6 bg-white rounded-2xl shadow-sm border border-gray-100 text-center">
  <% if current_user.habit %>
    <p class="text-gray-400 text-sm mb-1 italic">現在の習慣</p>
    <h1 class="text-2xl font-bold mb-8 text-gray-800">「<%= current_user.habit.name %>」</h1>

    <div class="space-y-4">
      <p class="text-sm text-gray-500">ここに3択ボタンを実装予定</p>
    </div>

    <div class="mt-10 pt-4 border-t border-gray-50">
      <%= link_to "習慣の設定・削除", edit_habit_path, class: "text-gray-400 text-xs hover:text-blue-500 underline transition" %>
    </div>
  <% else %>
    <h1 class="text-xl font-bold mb-4 text-gray-800">新しい習慣を始めましょう</h1>
    <p class="text-gray-500 text-sm mb-8">まずは1つだけ、無理なく続けられる目標を決めます。</p>
    <%= link_to "習慣を登録する", new_habit_path, class: "bg-blue-600 hover:bg-blue-700 text-white px-8 py-3 rounded-full font-bold shadow-md transition inline-block" %>
  <% end %>
</div>
