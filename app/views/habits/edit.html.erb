<div class="max-w-md mx-auto mt-10 p-6 bg-white rounded-2xl shadow-sm border border-gray-100">
  <h1 class="text-xl font-bold mb-6">習慣の設定</h1>
  <%= form_with model: @habit, url: habit_path, method: :patch, local: true, class: "space-y-6" do |f| %>
    <div>
      <%= f.label :name, "習慣の名前を変更", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <%= f.text_field :name, class: "w-full border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500" %>
    </div>
    <%= f.submit "更新する", class: "w-full bg-blue-600 text-white py-3 rounded-lg font-bold hover:bg-blue-700 transition" %>
  <% end %>

  <div class="mt-10 pt-6 border-t border-red-50">
    <p class="text-xs text-gray-400 mb-4 text-center">この習慣を終了しますか？（これまでの記録は履歴に残ります）</p>
    <%= button_to "習慣を削除する", habit_path, method: :delete, data: { confirm: "本当によろしいですか？" }, class: "w-full bg-white text-red-500 border border-red-200 py-2 rounded-lg text-sm font-medium hover:bg-red-50 transition" %>
  </div>
</div>
