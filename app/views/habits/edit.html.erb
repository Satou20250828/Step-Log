<div class="mx-auto mt-10 max-w-md rounded-2xl border border-gray-100 bg-white p-6 shadow-sm">
  <h1 class="mb-6 text-xl font-bold text-slate-900">習慣の設定</h1>
  <%= form_with model: @habit, url: habit_path, method: :patch, local: true, class: "space-y-6" do |f| %>
    <div>
      <%= f.label :name, "習慣の名前を変更", class: "mb-2 block text-sm font-medium text-gray-700" %>
      <%= f.text_field :name, class: "w-full rounded-lg border-gray-300 bg-white text-slate-900 placeholder:text-slate-400 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
    </div>
    <%= f.submit "更新する", class: "w-full bg-blue-600 text-white py-3 rounded-lg font-bold hover:bg-blue-700 transition" %>
  <% end %>

  <div class="mt-10 pt-6 border-t border-red-50">
    <p class="mb-4 text-center text-xs text-gray-500">この習慣を終了しますか？（これまでの記録は履歴に残ります）</p>
    <%= button_to "習慣を削除する", habit_path, method: :delete, data: { confirm: "本当によろしいですか？" }, class: "w-full bg-white text-red-500 border border-red-200 py-2 rounded-lg text-sm font-medium hover:bg-red-50 transition" %>
  </div>
</div>
